spring:
  application:
    name: Travelsystem
  datasource: 
	 driver-class-name: com.mysql.cj.jdbc.Driver
	 url: jdbc:mysql://localhost:3306/YBtest
	 username: root
	 password: root
  jpa:
    hibernate:
    ddl-auto: update
    show-sql: true
    database:
    database-platform: org.hibernate.dialect.H2Dialect
server: 
  port: 3306
